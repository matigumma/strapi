'use strict';

/**
 * professor-invitation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::professor-invitation.professor-invitation');
